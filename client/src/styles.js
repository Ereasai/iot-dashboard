const app = {
    display: 'flex'
}

const leftNavBar = {
    display: 'flex',
    background: 'red',
    height: '100vh',
    width: 70,
    flexDirection: 'column',
}

const widget = {
    
}


export default {
    app,
    leftNavBar,
    widget
}